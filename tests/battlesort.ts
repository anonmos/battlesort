import Assert from 'power-assert'
import {suite, suiteSetup, setup, test} from 'mocha'

suite('Tree tests', function() {
    suiteSetup(function() {
        // No setup for the moment
    })

    setup(function() {
        // No setup for the moment
    })

    test('A first, but wonderful, test', function() {
        Assert.ok(true)
    })
})